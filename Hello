-- Gui to Lua
-- Version: 3.2

-- Instances:

local test = Instance.new("ScreenGui")
local Background = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local ScrollingFrame = Instance.new("ScrollingFrame")
local UICorner_2 = Instance.new("UICorner")
local Playernamehere = Instance.new("TextButton")
local UICorner_3 = Instance.new("UICorner")

--Properties:

test.Name = "test"
test.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
test.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Background.Name = "Background"
Background.Parent = test
Background.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Background.BackgroundTransparency = 0.600
Background.BorderColor3 = Color3.fromRGB(0, 0, 0)
Background.BorderSizePixel = 0
Background.Position = UDim2.new(0.658131063, 0, 0.375562966, 0)
Background.Size = UDim2.new(0.271244854, 0, 0.339988738, 0)

UICorner.CornerRadius = UDim.new(0, 15)
UICorner.Parent = Background

ScrollingFrame.Parent = Background
ScrollingFrame.Active = true
ScrollingFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
ScrollingFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
ScrollingFrame.BorderSizePixel = 0
ScrollingFrame.Position = UDim2.new(0.0528217368, 0, 0.101883687, 0)
ScrollingFrame.Size = UDim2.new(0.895072281, 0, 0.793078661, 0)

UICorner_2.CornerRadius = UDim.new(0, 10)
UICorner_2.Parent = ScrollingFrame

Playernamehere.Name = "Playername here"
Playernamehere.Parent = ScrollingFrame
Playernamehere.BackgroundColor3 = Color3.fromRGB(131, 131, 131)
Playernamehere.BorderColor3 = Color3.fromRGB(0, 0, 0)
Playernamehere.BorderSizePixel = 0
Playernamehere.Position = UDim2.new(0.0283670239, 0, 0.0243751984, 0)
Playernamehere.Size = UDim2.new(0.924764991, 0, 0.0594427548, 0)
Playernamehere.Font = Enum.Font.SourceSansBold
Playernamehere.Text = ""
Playernamehere.TextColor3 = Color3.fromRGB(0, 0, 0)
Playernamehere.TextSize = 14.000

UICorner_3.Parent = Playernamehere

-- Scripts:

local function XCPUU_fake_script() -- ScrollingFrame.LocalScript 
	local script = Instance.new('LocalScript', ScrollingFrame)

	local script = Instance.new('LocalScript', ScrollingFrame)
	local Players = game:GetService("Players")
	local playerList = script.Parent
	local playerButtonTemplate = playerList:WaitForChild("Playername here")
	local layout = playerList:WaitForChild("UIListLayout")
	local selectedPlayer
	
	local function addPlayer(player)
		if playerList:FindFirstChild(player.Name) then
			return
		end
end
coroutine.wrap(XCPUU_fake_script)()
