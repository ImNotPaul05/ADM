local Players = game:GetService("Players")

-- Function to find and trigger the trade logic for a specific player name
local function triggerTradeForPlayer(playerName)
	local player = Players:FindFirstChild(playerName)
	if player then
		local playerGui = player:FindFirstChild("PlayerGui")
		if playerGui then
			local profileApp = playerGui:FindFirstChild("PlayerProfileApp")
			if profileApp then
				-- Assuming your button path follows the image structure
				local frame = profileApp:FindFirstChild("Frame")
				if frame then
					local header = frame:FindFirstChild("Header")
					if header then
						local tradeButton = header:FindFirstChild("TradeButton")
						if tradeButton then
							-- Option 1: Fire the Activated event programmatically if supported
							-- Option 2: Call the same function that your button's Activated event calls
							print("Found TradeButton for " .. playerName)
							
							-- If you have a bindable function or shared module logic tied to the button:
							-- executeTradeLogic(player)
						end
					end
				end
			end
		end
	end
end

-- Example usage:
-- triggerTradeForPlayer("TargetPlayerName")
